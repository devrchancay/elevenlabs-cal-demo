# Copy to .env and fill in. Vite only exposes variables prefixed with VITE_.

# Agent id. It lands in agent/agents.json after the first `elevenlabs agents push`.
VITE_ELEVENLABS_AGENT_ID=

# Public backend URL, no trailing slash.
VITE_BACKEND_URL=https://your-service.up.railway.app
